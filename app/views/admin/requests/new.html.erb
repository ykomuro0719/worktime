<h2>New</h2>

<%= form_for(@request, :url => admin_requests_path) do |f|  %>
        <%= f.label :requestTitle, "要求元" %>
        <%= f.text_field :requestTitle %>
<%= f.submit %>

<% end %>
<%= render 'footer' %>